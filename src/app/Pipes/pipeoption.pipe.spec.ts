import { PipeoptionPipe } from './pipeoption.pipe';

describe('PipeoptionPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeoptionPipe();
    expect(pipe).toBeTruthy();
  });
});
